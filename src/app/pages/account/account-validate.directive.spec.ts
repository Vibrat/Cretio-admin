import { AccountValidateDirective } from './account-validate.directive';

describe('AccountValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountValidateDirective();
    expect(directive).toBeTruthy();
  });
});
